use alienfile;

plugin PkgConfig => pkg_name => 'liburing', minimum_version => '0.3';

meta->prop->{out_of_source} = 1;

share {
  start_url 'extern/liburing-0.3.tar.gz';
  plugin 'Fetch::Local';
  plugin Extract => format => 'tar.gz';
  plugin 'Build::CMake';
  plugin 'Gather::IsolateDynamic';
};
